import { formHandler } from '../js/formHandler';

describe('Handle submission for geoNames Weatherbit and Pixabay API', () => {
    test('grab all API info & handle submission functions', () => {
        expect(formHandler).toBeDefined();
    });
});